import 'package:flutter/material.dart';

class Concept2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Concept 2 data"),
      ),
      body: Text("Under construction"),
    );
  }
}
